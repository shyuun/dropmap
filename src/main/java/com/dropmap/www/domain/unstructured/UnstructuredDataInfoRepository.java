package com.dropmap.www.domain.unstructured;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UnstructuredDataInfoRepository extends JpaRepository<UnstructuredDataInfo, Long> {
}
